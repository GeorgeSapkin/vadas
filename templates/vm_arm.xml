<domain type='qemu'>
  <name>{{VM_NAME}}</name>
  <metadata>
    <libosinfo:libosinfo xmlns:libosinfo="http://libosinfo.org/xmlns/libvirt/domain/1.0">
      <libosinfo:os id="http://libosinfo.org/linux/2024"/>
    </libosinfo:libosinfo>
    <vadas:tags xmlns:vadas="urn:vadas">
      <vadas:wanip>{{NET_WAN_IP}}</vadas:wanip>
      <vadas:lanip>{{NET_LAN_IP}}</vadas:lanip>
    </vadas:tags>
  </metadata>
  <memory unit='KiB'>{{VM_RAM}}</memory>
  <vcpu placement='static'>{{VM_CORES}}</vcpu>
  <os firmware="efi">
    <type arch='{{ARCH}}' machine='{{MACHINE}}'>hvm</type>
    <firmware>
      <feature enabled="no" name="enrolled-keys"/>
      <feature enabled="no" name="secure-boot"/>
    </firmware>
    <loader readonly="yes" type="pflash" format="qcow2">{{LOADER}}</loader>
    <nvram template="{{NVRAM_TEMPLATE}}" templateFormat="qcow2" format="qcow2">{{NVRAM_FILE}}</nvram>
    <boot dev='hd'/>
  </os>
  <features>
    <acpi/>
    <gic version="2"/>
  </features>
  <cpu mode="custom" match="exact" check="none">
    <model fallback="allow">cortex-a57</model>
  </cpu>
  <devices>
    <emulator>{{EMULATOR}}</emulator>
    <disk type='file' device='disk'>
      <driver name='qemu' type='raw'/>
      <source file='{{IMAGE}}'/>
      <target dev='vda' bus='virtio'/>
    </disk>
    <interface type='network'>
      <source network='{{NET_WAN_NAME}}'/>
      <model type='virtio'/>
    </interface>
    <interface type='network'>
      <source network='{{NET_LAN_NAME}}'/>
      <model type='virtio'/>
    </interface>
    <console type='pty'>
      <target type='serial' port='0'/>
    </console>
    <graphics type='spice' autoport='yes'>
      <listen type='address'/>
      <image compression="off"/>
    </graphics>
    <video>
      <model type="virtio" heads="1" primary="yes"/>
    </video>
    <controller type="virtio-serial" index="0"/>
    <channel type="unix">
      <target type="virtio" name="org.qemu.guest_agent.0"/>
      <address type="virtio-serial" controller="0" bus="0" port="1"/>
    </channel>
    <memballoon model="virtio"/>
    <rng model="virtio">
      <backend model="random">/dev/urandom</backend>
    </rng>
  </devices>
</domain>
