<domain type='kvm'>
  <name>{{VM_NAME}}</name>
  <metadata>
    <libosinfo:libosinfo xmlns:libosinfo="http://libosinfo.org/xmlns/libvirt/domain/1.0">
      <libosinfo:os id="http://libosinfo.org/linux/2024"/>
    </libosinfo:libosinfo>
    <vadas:tags xmlns:vadas="urn:vadas">
      <vadas:wanip>{{NET_WAN_IP}}</vadas:wanip>
      <vadas:lanip>{{NET_LAN_IP}}</vadas:lanip>
    </vadas:tags>
  </metadata>
  <memory unit='KiB'>{{VM_RAM}}</memory>
  <vcpu placement='static'>{{VM_CORES}}</vcpu>
  <os>
    <type arch='{{ARCH}}' machine='{{MACHINE}}'>hvm</type>
    <boot dev='hd'/>
  </os>
  <features>
    <acpi/>
    <apic/>
  </features>
  <devices>
    <emulator>{{EMULATOR}}</emulator>
    <disk type='file' device='disk'>
      <driver name='qemu' type='raw'/>
      <source file='{{IMAGE}}'/>
      <target dev='vda' bus='virtio'/>
    </disk>
    <interface type='network'>
      <source network='{{NET_WAN_NAME}}'/>
      <model type='virtio'/>
    </interface>
    <interface type='network'>
      <source network='{{NET_LAN_NAME}}'/>
      <model type='virtio'/>
    </interface>
    <console type='pty'>
      <target type='serial' port='0'/>
    </console>
    <graphics type='spice' autoport='yes'>
      <listen type='address'/>
    </graphics>
    <video>
      <model type='virtio'/>
    </video>
    <memballoon model="virtio"/>
    <rng model="virtio">
      <backend model="random">/dev/urandom</backend>
    </rng>
  </devices>
</domain>
